export const rootRoutes = {
    splash: 'splash',
    auth: 'auth'
} as const

export const authRoutes = {
    home: 'home',
    login: 'login'
} as const

export const appRoutes = {
    home: 'home'
} as const